import React from 'react';
import GoogleButton from '../../shared/components/google-button.component';

const LoginContainer: React.FC = () => {
  return (
    <div>
      <h2>Signin Container Component</h2>
      <GoogleButton />
    </div>
  );
};

export default LoginContainer;
